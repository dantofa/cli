# .trivyignore-cluster -- accepted HIGH/CRITICAL CVEs for the platform's running
# images, enforced by `just verify-image-scan` (the preview image-security gate).
# One CVE ID per line; `#` comments. Every entry needs a reason and, where it is
# waiting on an upstream fix, a corresponding ROADMAP.md tracking item so it gets
# removed once patched. Any new unlisted HIGH/CRITICAL finding fails the gate.
#
# NOTE: seeded from ROADMAP's tracked CVEs for charts deployed here; the first
# preview run surfaces the full current set (the gate prints every offending
# CVE + image), which then completes this file.

# grpc -- present in the Flux and trivy-operator chart images; awaiting upstream
# patch (tracked in ROADMAP.md). Remove when the charts ship the fix.
CVE-2026-33186

# OpenSSL (Alpine base) in the trivy-operator image; awaiting upstream patch
# (tracked in ROADMAP.md). Remove when the chart ships the fix.
CVE-2026-31789
